services:
  mongo:
    image: mongo:7
    container_name: bizscout-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    environment:
      MONGO_INITDB_DATABASE: bizscout

volumes:
  mongo-data:
